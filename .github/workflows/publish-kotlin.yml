name: Publish Kotlin SDK

on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Publish to Maven Central
        uses: gradle/gradle-build-action@v2
        with:
          build-root-directory: packages/kotlin
          arguments: >
            publishAllPublicationsToMavenCentralRepository
            -PmavenCentralUsername=${{ secrets.MAVEN_USERNAME }}
            -PmavenCentralPassword=${{ secrets.MAVEN_PASSWORD }}
            -Psigning.keyId=${{ secrets.GPG_KEY_ID }}
            -Psigning.password=${{ secrets.GPG_SIGNING_PASSWORD }}
            -Psigning.secretKey=${{ secrets.GPG_SIGNING_KEY }}
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
          GPG_SIGNING_PASSWORD: ${{ secrets.GPG_SIGNING_PASSWORD }}
          GPG_SIGNING_KEY: ${{ secrets.GPG_SIGNING_KEY }}
